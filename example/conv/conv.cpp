#include "RainNet.h"

// Layer* layer_0 = new ConvolutionalLayer(5, 5, 3, 0, 0, "sigmoid");
// Layer* layer_1 = new ConvolutionalLayer(3, 3, 3, 1, 1, "sigmoid");

// float* input = (float*) new float[5 * 5 * 3];
// float* target = (float*) new float[5 * 5 *3];

int main()
{
    // Net network;
    // network.learningRate = 1.0;
    // network.layers.push_back(layer_0);
    // network.layers.push_back(layer_1);

    // for (size_t i=0; i<5*5*3; ++i)
    //     input[i] = 1;

    // for (size_t i=0; i<5*5*3; ++i)
    //     target[i] = 1;

    // size_t epoch = 1;
    // network.train(input, epoch);

    return 0;
}